<Window x:Class="PetHelper.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="220" Height="260" WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        LocationChanged="Window_LocationChanged">
  <Window.Resources>
    <Style x:Key="PetContextMenu" TargetType="{x:Type ContextMenu}">
      <Setter Property="Background" Value="#FFF8F9FC" />
      <Setter Property="BorderBrush" Value="#140F172A" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Padding" Value="6" />
      <Setter Property="HasDropShadow" Value="True" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type ContextMenu}">
            <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="14" Padding="{TemplateBinding Padding}">
              <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
            </Border>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="PetContextMenuItem" TargetType="{x:Type MenuItem}">
      <Setter Property="Foreground" Value="#344054" />
      <Setter Property="FontSize" Value="13" />
      <Setter Property="Padding" Value="10,7" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type MenuItem}">
            <Grid>
              <Border x:Name="Row" Background="Transparent" CornerRadius="8" Padding="{TemplateBinding Padding}">
                <Grid>
                  <ContentPresenter ContentSource="Header" VerticalAlignment="Center" />
                  <TextBlock x:Name="SubmenuArrow" Text="›" Foreground="#98A2B3" FontSize="18"
                             HorizontalAlignment="Right" VerticalAlignment="Center" Visibility="Collapsed" />
                </Grid>
              </Border>
              <Popup x:Name="PART_Popup" IsOpen="{TemplateBinding IsSubmenuOpen}" Placement="Right"
                     AllowsTransparency="True" PopupAnimation="Fade" Focusable="False">
                <Border Background="#FFF8F9FC" BorderBrush="#140F172A" BorderThickness="1" CornerRadius="14" Padding="6">
                  <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Cycle" />
                </Border>
              </Popup>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Row" Property="Background" Value="#14000000" />
              </Trigger>
              <Trigger Property="Role" Value="SubmenuHeader">
                <Setter TargetName="SubmenuArrow" Property="Visibility" Value="Visible" />
              </Trigger>
              <Trigger Property="Role" Value="TopLevelHeader">
                <Setter TargetName="SubmenuArrow" Property="Visibility" Value="Visible" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="PetContextMenuSeparator" TargetType="{x:Type Separator}">
      <Setter Property="Margin" Value="6,4" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="{x:Type Separator}">
            <Border Height="1" Background="#120F172A" />
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>
  <Window.ContextMenu>
    <ContextMenu Style="{StaticResource PetContextMenu}">
      <ContextMenu.Resources>
        <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource PetContextMenuItem}" />
        <Style TargetType="{x:Type Separator}" BasedOn="{StaticResource PetContextMenuSeparator}" />
      </ContextMenu.Resources>
      <MenuItem Header="选择会话" Click="SelectTargetMenuItem_Click" />
      <MenuItem Header="隐藏" Click="HideMenuItem_Click" />
      <MenuItem Header="缩放">
        <MenuItem Header="75%" Tag="0.75" Click="ScaleMenuItem_Click" />
        <MenuItem Header="100%" Tag="1" Click="ScaleMenuItem_Click" />
        <MenuItem Header="125%" Tag="1.25" Click="ScaleMenuItem_Click" />
        <MenuItem Header="150%" Tag="1.5" Click="ScaleMenuItem_Click" />
      </MenuItem>
      <Separator />
      <MenuItem Header="重置大小与位置" Click="ResetMenuItem_Click" />
      <MenuItem Header="本次关闭桌宠" Click="CloseMenuItem_Click" />
    </ContextMenu>
  </Window.ContextMenu>
  <Grid x:Name="PetLayout" MouseLeftButtonDown="Pet_MouseLeftButtonDown"
        MouseMove="Pet_MouseMove" MouseLeftButtonUp="Pet_MouseLeftButtonUp"
        LostMouseCapture="Pet_LostMouseCapture" SizeChanged="PetLayout_SizeChanged" AllowDrop="True"
        DragEnter="Pet_DragEnter" DragOver="Pet_DragOver" Drop="Pet_Drop">
    <Image x:Name="PetImage" Stretch="Uniform" VerticalAlignment="Bottom" Margin="0,20,0,0" />
    <Canvas x:Name="StateBubbleCanvas" IsHitTestVisible="False" Panel.ZIndex="1">
      <Border x:Name="StateBubble" Visibility="Collapsed" Background="#E62B4B5F"
              CornerRadius="12" Padding="12,6" SizeChanged="StateBubble_SizeChanged">
        <TextBlock x:Name="StateLabel" Foreground="White" FontWeight="SemiBold" TextWrapping="Wrap" />
      </Border>
    </Canvas>
  </Grid>
</Window>
