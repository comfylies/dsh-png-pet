<Window x:Class="PetHelper.TargetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="360" Height="480" WindowStartupLocation="Manual"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        AllowDrop="True"
        DragEnter="TargetWindow_DragEnter" DragOver="TargetWindow_DragOver" Drop="TargetWindow_Drop"
        MouseLeftButtonDown="TargetWindow_MouseLeftButtonDown">
  <Window.Resources>
    <Style x:Key="TargetButton" TargetType="{x:Type Button}">
      <Setter Property="Background" Value="#101828" /><Setter Property="Foreground" Value="White" />
      <Setter Property="BorderThickness" Value="0" /><Setter Property="Cursor" Value="Hand" />
      <Setter Property="FontWeight" Value="SemiBold" /><Setter Property="Padding" Value="12,7" />
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="{x:Type Button}">
        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10" Padding="{TemplateBinding Padding}">
          <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
        <ControlTemplate.Triggers>
          <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="Background" Value="#344054" /></Trigger>
          <Trigger Property="IsPressed" Value="True"><Setter TargetName="border" Property="Background" Value="#000000" /></Trigger>
          <Trigger Property="IsEnabled" Value="False"><Setter TargetName="border" Property="Background" Value="#D0D5DD" /></Trigger>
        </ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style x:Key="GhostButton" TargetType="{x:Type Button}">
      <Setter Property="Background" Value="Transparent" /><Setter Property="Foreground" Value="#475467" />
      <Setter Property="BorderThickness" Value="0" /><Setter Property="Cursor" Value="Hand" /><Setter Property="Padding" Value="8,5" />
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="{x:Type Button}">
        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
          <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Border>
        <ControlTemplate.Triggers>
          <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="border" Property="Background" Value="#14000000" /></Trigger>
          <Trigger Property="IsPressed" Value="True"><Setter TargetName="border" Property="Background" Value="#22000000" /></Trigger>
        </ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style x:Key="TargetRow" TargetType="{x:Type ListBoxItem}">
      <Setter Property="Padding" Value="10,7" /><Setter Property="Margin" Value="0,1" /><Setter Property="HorizontalContentAlignment" Value="Stretch" />
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="{x:Type ListBoxItem}">
        <Border x:Name="row" Background="Transparent" CornerRadius="8" Padding="{TemplateBinding Padding}"><ContentPresenter /></Border>
        <ControlTemplate.Triggers>
          <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="row" Property="Background" Value="#14000000" /></Trigger>
          <Trigger Property="IsSelected" Value="True"><Setter TargetName="row" Property="Background" Value="#22000000" /></Trigger>
        </ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
    <Style x:Key="WorkspaceAccordion" TargetType="{x:Type Expander}">
      <Setter Property="Margin" Value="0,0,0,4" />
      <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="{x:Type Expander}">
        <StackPanel>
          <ToggleButton x:Name="HeaderSite" Content="{TemplateBinding Header}" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}"
                        Background="Transparent" BorderThickness="0" Padding="0" FocusVisualStyle="{x:Null}">
            <ToggleButton.Template><ControlTemplate TargetType="{x:Type ToggleButton}">
              <Border x:Name="header" Background="Transparent" CornerRadius="10" Padding="10,8">
                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="18" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
                  <Path x:Name="Chevron" Grid.Column="0" Data="M 5,2 L 10,7 L 5,12" Stroke="#667085" StrokeThickness="1.6"
                        StrokeStartLineCap="Round" StrokeEndLineCap="Round" HorizontalAlignment="Center" VerticalAlignment="Center" RenderTransformOrigin="0.5,0.5"><Path.RenderTransform><RotateTransform Angle="0" /></Path.RenderTransform></Path>
                  <ContentPresenter Grid.Column="1" Content="{TemplateBinding Content}" VerticalAlignment="Center" />
                </Grid>
              </Border>
              <ControlTemplate.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="header" Property="Background" Value="#14000000" /></Trigger>
                <Trigger Property="IsChecked" Value="True"><Setter TargetName="Chevron" Property="RenderTransform"><Setter.Value><RotateTransform Angle="90" /></Setter.Value></Setter><Setter TargetName="header" Property="Background" Value="#0D000000" /></Trigger>
              </ControlTemplate.Triggers>
            </ControlTemplate></ToggleButton.Template>
          </ToggleButton>
          <ContentPresenter x:Name="ContentSite" ContentSource="Content" Margin="18,2,0,4" Visibility="Collapsed" />
        </StackPanel>
        <ControlTemplate.Triggers><Trigger Property="IsExpanded" Value="True"><Setter TargetName="ContentSite" Property="Visibility" Value="Visible" /></Trigger></ControlTemplate.Triggers>
      </ControlTemplate></Setter.Value></Setter>
    </Style>
  </Window.Resources>
  <Border Background="#F0FFFFFF" CornerRadius="20" BorderBrush="#140F172A" BorderThickness="1" Margin="1">
    <Grid Margin="16"><Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
      <Grid Grid.Row="0" Height="28">
        <Button x:Name="BackButton" Width="28" Height="28" Content="←" Style="{StaticResource GhostButton}" Visibility="Collapsed" Click="BackButton_Click" ToolTip="返回会话列表" />
        <TextBlock x:Name="CardTitle" Text="选择会话" Foreground="#344054" FontWeight="SemiBold" FontSize="13" Margin="2,0,34,0" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" />
        <Button HorizontalAlignment="Right" Width="28" Height="28" Content="×" Style="{StaticResource GhostButton}" FontSize="18" Click="CloseButton_Click" ToolTip="关闭" />
      </Grid>
      <Grid Grid.Row="1" Margin="0,10,0,0">
        <TextBlock x:Name="LoadingView" Text="加载中…" Foreground="#667085" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <StackPanel x:Name="ErrorView" Visibility="Collapsed" HorizontalAlignment="Center" VerticalAlignment="Center">
          <TextBlock x:Name="ErrorLabel" Foreground="#667085" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="280" />
          <Button Content="重试" Style="{StaticResource TargetButton}" Margin="0,10,0,0" HorizontalAlignment="Center" Click="RetryButton_Click" />
        </StackPanel>
        <Grid x:Name="TargetTreeView" Visibility="Collapsed"><Grid.RowDefinitions><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
          <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled"><StackPanel>
            <TextBlock Text="工作区" Foreground="#667085" FontSize="11" Margin="10,0,0,6" />
            <ItemsControl x:Name="WorkspaceTree"><ItemsControl.ItemTemplate><DataTemplate>
              <Expander Style="{StaticResource WorkspaceAccordion}" IsExpanded="{Binding IsExpanded, Mode=TwoWay}">
                <Expander.Header><Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><TextBlock Text="{Binding Workspace.Title}" Foreground="#344054" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" /><TextBlock Grid.Column="1" Text="{Binding SessionCount, StringFormat={}{0} 个会话}" Foreground="#98A2B3" FontSize="11" /></Grid></Expander.Header>
                <StackPanel>
                  <ListBox ItemsSource="{Binding Sessions}" BorderThickness="0" Background="Transparent" ItemContainerStyle="{StaticResource TargetRow}" SelectionChanged="NestedSessionList_SelectionChanged"><ListBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding DisplayTitle}" Foreground="#475467" TextTrimming="CharacterEllipsis" /></DataTemplate></ListBox.ItemTemplate></ListBox>
                  <Button Content="＋ 新对话" Tag="{Binding Workspace.Id}" Style="{StaticResource GhostButton}" HorizontalAlignment="Left" Margin="0,4,0,2" Click="NewSessionButton_Click" />
                </StackPanel>
              </Expander>
            </DataTemplate></ItemsControl.ItemTemplate></ItemsControl>
            <Expander x:Name="UngroupedExpander" Style="{StaticResource WorkspaceAccordion}" Visibility="Collapsed" IsExpanded="False">
              <Expander.Header><TextBlock Text="未分组会话" Foreground="#344054" FontWeight="SemiBold" /></Expander.Header>
              <StackPanel>
                <ListBox x:Name="UngroupedSessionList" BorderThickness="0" Background="Transparent" ItemContainerStyle="{StaticResource TargetRow}" SelectionChanged="NestedSessionList_SelectionChanged"><ListBox.ItemTemplate><DataTemplate><TextBlock Text="{Binding DisplayTitle}" Foreground="#475467" TextTrimming="CharacterEllipsis" /></DataTemplate></ListBox.ItemTemplate></ListBox>
                <Button Content="＋ 新对话" Style="{StaticResource GhostButton}" HorizontalAlignment="Left" Margin="0,4,0,2" Click="NewSessionButton_Click" />
              </StackPanel>
            </Expander>
          </StackPanel></ScrollViewer>
          <StackPanel Grid.Row="1" Margin="0,10,0,0"><TextBlock Text="拖入文件夹可直接新建工作区" Foreground="#98A2B3" FontSize="11" Margin="2,0,0,6" /><Button Content="＋ 新建工作区" Style="{StaticResource TargetButton}" Click="CreateWorkspaceButton_Click" /></StackPanel>
        </Grid>
        <Grid x:Name="CreateView" Visibility="Collapsed"><StackPanel><TextBlock Text="选择一个已存在的目录，或将文件夹拖入此窗口。" Foreground="#667085" FontSize="12" TextWrapping="Wrap" Margin="2,0,0,10" /><Button Content="浏览目录…" Style="{StaticResource TargetButton}" HorizontalAlignment="Left" Click="BrowseButton_Click" /></StackPanel></Grid>
      </Grid>
    </Grid>
  </Border>
</Window>
