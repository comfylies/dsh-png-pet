# 外置人物库实施记录

依据 `../specs/2026-09-10-external-character-library-design.md`，在用户既有 v0.2.10 对话窗口改动之上实现；不覆盖已有改动，不扩展 JSON Lines。

- [x] 补充人物来源清单、受控导入、选择恢复、逐帧时长测试，先验证缺失实现的失败。
- [x] 实现严格来源/存储格式，随机人物 ID、独立目录、取消与失败清理、原子选择记录、文件锁。
- [x] 实现 GIF 签名/结构预检、局部帧合成、透明与 disposal、PNG 导入、方形画布规范化和资源限额。
- [x] 为播放器增加外置来源、逐帧时长、末帧完整停留、按解码字节限制缓存；回归内置编排。
- [x] 添加 WPF 人物管理窗口和菜单，预览、名称/锚点/基线调整、切换、恢复默认、删除与启动恢复。
- [x] 补充来源路径、篡改、GIF 合成和窗口离屏布局检查；新增 tgz/EXE 不含外置角色的检查。
- [x] 更新需求边界和用户说明，版本递增至 0.2.11。
- [x] 完整 Node/C# 验证、自包含 Helper 构建、包测试与本地发布包生成。

验证：228 项 Node 测试、342 项 C# 测试及包测试全部通过；人物窗口已做离屏渲染检查。产物为 `dist/packages/dsh-png-pet-0.2.11.tgz`，同时生成 `install.ps1` 和 `release.json` 并校验 SHA-256，目录保留最新五个版本包。

本机发布尾步骤遇到嵌套 Windows PowerShell 无法找到 Get-FileHash；测试、构建、包检查和 npm pack 均已成功。使用当前 PowerShell 单独执行同一 `scripts/new-installer-release.ps1` 完成安装清单生成并验证哈希，未为本机模块环境问题改动发布脚本。

本地发布包只输出到 `dist/packages`；不自动改动已安装的 DSH 插件或运行中的桌宠。
